name: Deep Gate (manual)

# Manually-triggered deep gate (workflow_dispatch ONLY — no cron / schedule, per
# docs/archive/ci_cd/CI_CD_IMPROVEMENT_PLAN.md §0). Runs the slow / environment-sensitive checks
# that deliberately do NOT gate every PR: full E2E incl. accessibility, the
# cold-start + old-DB migration smokes, and the informational dependency scan
# (moved here from the PR pipeline). Trigger from the Actions tab or with
# `gh workflow run deep-gate.yml --ref <branch>`.
#
# Visual regression (Phase 4.2) runs as two opt-in jobs below — `visual-linux`
# behind `run_visual`, and `visual-windows` behind `run_visual_windows`. Both are
# manual-only and NEITHER is a required PR check.
# The committed PNGs are split by platform (e2e/__screenshots__/{win32,linux}/).
# The two platforms are NOT symmetric, and the asymmetry is deliberate:
#   linux  — generated by this workflow's `generate` mode on ubuntu-24.04, then
#            committed by the owner after review (docs/archive/ci_cd/phase4_2/PLANNING.md).
#   win32  — generated on the owner's LOCAL Windows machine (#309) and committed
#            after a by-eye review. `visual-windows` is COMPARE-ONLY and has no
#            generate mode, so it can measure that corpus but never produce it.
# Consequence worth knowing before reading a red: a win32 compare on a runner is
# comparing runner rendering against locally-generated pixels.
on:
  workflow_dispatch:
    inputs:
      run_visual:
        description: 'Run the Linux visual regression job'
        type: boolean
        default: false
      visual_mode:
        description: 'Visual mode: compare against committed Linux baselines, or generate new ones'
        type: choice
        options: [compare, generate]
        default: compare
      run_visual_windows:
        description: 'Run the Windows visual regression job (compare-only, never generates)'
        type: boolean
        default: false

jobs:
  full-e2e:
    name: Full E2E incl. accessibility (Chromium)
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version-file: '.python-version'
        cache: 'pip'

    - name: Set up Node.js
      uses: actions/setup-node@v7
      with:
        node-version: '24'
        cache: 'npm'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Install npm dependencies
      run: npm ci

    - name: Build Bootstrap-customized CSS
      run: npm run build:css

    - name: Install Playwright Chromium
      run: npx playwright install --with-deps chromium

    # Run every spec EXCEPT the two visual ones (visual.spec.ts,
    # visual-baseline-thumbnails.spec.ts) — their committed baselines are
    # Windows-rendered and diff ~100% on ubuntu until Phase 4.2 lands a
    # Linux baseline set. Everything else runs, accessibility included; this is a
    # manual gate, so a human reads the report (known-red nav-dropdown may fail).
    # workout-plan-desktop-contract.spec.ts is excluded for the same reason as
    # the two snapshot specs: it needs the plan-bearing visual seed, and this
    # job runs on the user-state-wiped functional one. It runs in visual-linux.
    - name: Run full suite (minus visual)
      run: |
        SPECS=$(ls e2e/*.spec.ts | grep -vE 'visual\.spec\.ts|visual-baseline-thumbnails\.spec\.ts|workout-plan-desktop-contract\.spec\.ts' | tr '\n' ' ')
        echo "Running: $SPECS"
        npx playwright test --project=chromium $SPECS

    - name: Upload Playwright artifacts
      if: failure()
      uses: actions/upload-artifact@v7
      with:
        name: deep-gate-full-e2e
        path: artifacts/playwright
        retention-days: 7

  first-install:
    name: First install (catalog seed) smoke
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version-file: '.python-version'
        cache: 'pip'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # Fresh-clone / first-run path: the real app.py startup must copy the
    # immutable catalog seed into a missing configured runtime DB.
    - name: Boot missing runtime DB and assert seeded catalog
      run: |
        export DB_FILE=artifacts/deep-gate/first-install.db
        mkdir -p "$(dirname "$DB_FILE")"
        rm -f "$DB_FILE" "$DB_FILE-wal" "$DB_FILE-shm" "$DB_FILE-journal"
        DB_FILE="$DB_FILE" FLASK_USE_RELOADER=0 python app.py &
        APP_PID=$!
        code=000
        for _ in $(seq 1 30); do
          code=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5000/ || echo 000)
          [ "$code" = "200" ] && break
          sleep 2
        done
        kill "$APP_PID" 2>/dev/null || true
        echo "first install: GET / -> $code"
        [ "$code" = "200" ]
        python - <<'PY'
        import os
        import sqlite3

        from utils.schema_registry import OWNED_TABLES_DROP_ORDER

        con = sqlite3.connect(os.environ["DB_FILE"])
        try:
            exercises = con.execute("SELECT COUNT(*) FROM exercises").fetchone()[0]
            assert exercises == 1897, exercises
            for table in OWNED_TABLES_DROP_ORDER:
                count = con.execute(
                    f'SELECT COUNT(*) FROM "{table}"'
                ).fetchone()[0]
                assert count == 0, (table, count)
        finally:
            con.close()
        PY

  empty-schema:
    name: Empty-schema initializer smoke
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version-file: '.python-version'
        cache: 'pip'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # Bypass app.py's first-install bootstrap deliberately. This preserves the
    # independent contract that initializers can create a valid empty schema.
    - name: Initialize an empty DB, boot it, and assert GET / -> 200
      run: |
        export DB_FILE=artifacts/deep-gate/empty-schema.db
        mkdir -p "$(dirname "$DB_FILE")"
        rm -f "$DB_FILE" "$DB_FILE-wal" "$DB_FILE-shm" "$DB_FILE-journal"
        python - <<'PY'
        import sqlite3

        import utils.config
        from utils.schema_registry import run_all_initializers

        run_all_initializers(force_base=True)
        con = sqlite3.connect(utils.config.DB_FILE)
        try:
            exercises = con.execute("SELECT COUNT(*) FROM exercises").fetchone()[0]
            assert exercises == 0, exercises
        finally:
            con.close()
        PY
        DB_FILE="$DB_FILE" FLASK_USE_RELOADER=0 python app.py &
        APP_PID=$!
        code=000
        for _ in $(seq 1 30); do
          code=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5000/ || echo 000)
          [ "$code" = "200" ] && break
          sleep 2
        done
        kill "$APP_PID" 2>/dev/null || true
        echo "empty schema: GET / -> $code"
        [ "$code" = "200" ]

  old-db-migration:
    name: Old-DB migration compatibility
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version-file: '.python-version'
        cache: 'pip'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # Generate a pre-migration DB (no exercise_order, no add_*_table tables) with a
    # seeded program row, then boot the real app.py. The startup ALTERs / add_*_table
    # helpers must upgrade it in place and serve both the welcome and plan pages.
    - name: Generate historical-schema DB, boot, assert upgrade
      run: |
        export DB_FILE=artifacts/deep-gate/old-schema.db
        python tests/fixtures/make_old_schema_db.py --output "$DB_FILE"
        DB_FILE="$DB_FILE" FLASK_USE_RELOADER=0 python app.py &
        APP_PID=$!
        root=000
        for _ in $(seq 1 30); do
          root=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5000/ || echo 000)
          [ "$root" = "200" ] && break
          sleep 2
        done
        plan=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5000/workout_plan || echo 000)
        kill "$APP_PID" 2>/dev/null || true
        echo "old-DB migration: GET / -> $root ; GET /workout_plan -> $plan"
        [ "$root" = "200" ] && [ "$plan" = "200" ]
        python - <<'PY'
        import os
        import sqlite3

        db_file = os.environ["DB_FILE"]
        con = sqlite3.connect(db_file)
        try:
            columns = {
                row[1]
                for row in con.execute("PRAGMA table_info(user_selection)").fetchall()
            }
            required_columns = {
                "exercise_order",
                "superset_group",
                "execution_style",
                "time_cap_seconds",
            }
            missing_columns = sorted(required_columns - columns)
            if missing_columns:
                raise SystemExit(f"missing migrated columns: {missing_columns}")

            required_tables = {
                "progression_goals",
                "volume_plans",
                "muscle_volumes",
                "user_profile",
                "user_profile_lifts",
                "user_profile_preferences",
                "body_composition_snapshots",
                "learned_strength_calibrations",
                "user_calibration_settings",
                "program_backups",
                "program_backup_items",
            }
            tables = {
                row[0]
                for row in con.execute(
                    "SELECT name FROM sqlite_master WHERE type='table'"
                ).fetchall()
            }
            missing_tables = sorted(required_tables - tables)
            if missing_tables:
                raise SystemExit(f"missing migrated tables: {missing_tables}")

            row = con.execute(
                """
                SELECT routine, exercise, sets, min_rep_range, max_rep_range, rir, rpe, weight
                FROM user_selection
                WHERE routine = ? AND exercise = ?
                """,
                ("Legacy Routine", "Legacy Bench Press"),
            ).fetchone()
            expected = ("Legacy Routine", "Legacy Bench Press", 3, 6, 8, 2, 8.0, 100.0)
            if row != expected:
                raise SystemExit(f"legacy row mismatch: {row!r}")
        finally:
            con.close()
        PY

  frozen-windows:
    name: Frozen executable (real bootloader, Windows)
    # The half the development machine cannot run. Smart App Control there
    # refuses to launch a freshly built, unsigned bootloader (WinError 4551),
    # so the local smoke falls back to --mode payload, which exercises the
    # packaged files but NOT the bootloader. GitHub's Windows runners do not
    # enforce Smart App Control, so this job launches the executable an end
    # user actually double-clicks. Disabling Smart App Control locally is not
    # an option that was ever on the table.
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version-file: '.python-version'
        cache: 'pip'

    - name: Install runtime and pinned build dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-build.txt
        python -c "import PyInstaller; print('PyInstaller ' + PyInstaller.__version__)"

    # Staging is what the spec calls too; running it first fails a broken
    # manifest in seconds instead of after a full build, and writes the
    # manifest.sha256 the smoke verifies the built tree against.
    - name: Stage tracked package assets
      run: python scripts/stage_package_assets.py

    - name: Build via the canonical spec
      run: pyinstaller --clean --noconfirm Hypertrophy-Toolbox.spec

    # --mode bootloader is the entire point of this job. Do not weaken it to
    # payload mode to make a red run green: a payload pass says nothing about
    # whether the executable starts.
    - name: Smoke the real bootloader
      run: |
        python scripts/smoke_packaged_app.py --mode bootloader

    - name: Upload distribution inventory on failure
      if: failure()
      run: |
        Get-ChildItem -Recurse dist/Hypertrophy-Toolbox/_internal/data |
          Select-Object FullName, Length | Format-Table -AutoSize
      shell: pwsh

  visual-linux:
    name: Visual regression (Linux baselines)
    # Opt-in only: a normal deep-gate run skips this unless run_visual=true. This
    # is NOT a required PR check (workflow_dispatch-only; an if:-gated job cannot
    # block merge). Visual specs stay manual deep gate, per Phase 4.2.
    if: ${{ inputs.run_visual }}
    # PINNED to ubuntu-24.04 (NOT ubuntu-latest): the generate and compare runs
    # must share one runner image so the renderer (Chromium build + bundled fonts
    # + freetype) matches by construction. A GitHub image promotion must not
    # silently move fonts and red the gate. This MUST match the image used to
    # generate the committed e2e/__screenshots__/linux/ baselines.
    runs-on: ubuntu-24.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version-file: '.python-version'
        cache: 'pip'

    - name: Set up Node.js
      uses: actions/setup-node@v7
      with:
        node-version: '24'
        cache: 'npm'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Install npm dependencies
      run: npm ci

    - name: Build Bootstrap-customized CSS
      run: npm run build:css

    - name: Install Playwright Chromium
      run: npx playwright install --with-deps chromium

    # Fail fast if the committed visual seed is missing — prepare_visual_db.py
    # would otherwise fall back to data/database.db (DEFAULT_SOURCE) and could
    # render real data into a baseline artifact.
    - name: Assert committed visual seed present
      run: test -f e2e/fixtures/database.visual.seed.db

    # Run ONLY the two visual specs. PW_VISUAL_SEED=1 makes playwright.config.ts
    # seed the throwaway DB with the plan-bearing visual seed (prepare_visual_db.py)
    # in the webServer command before Flask opens it — no per-spec runtime DB
    # rewrite. generate mode writes baselines under e2e/__screenshots__/linux/
    # (via the {platform} snapshotPathTemplate); compare diffs against them.
    - name: Run visual specs
      env:
        PW_VISUAL_SEED: '1'
      run: |
        MODE="${{ inputs.visual_mode }}"
        UPDATE=""; [ "$MODE" = "generate" ] && UPDATE="--update-snapshots"
        echo "Visual mode: $MODE"
        npx playwright test --project=chromium \
          e2e/visual.spec.ts e2e/visual-baseline-thumbnails.spec.ts \
          e2e/workout-plan-desktop-contract.spec.ts $UPDATE

    # generate → upload the freshly rendered Linux baselines for owner review+commit.
    # CI never pushes to main (branch-protected) — every pixel change is committed
    # by a human after downloading and inspecting this artifact.
    - name: Upload generated Linux baselines
      if: ${{ inputs.visual_mode == 'generate' }}
      uses: actions/upload-artifact@v7
      with:
        name: visual-baselines-linux
        path: e2e/__screenshots__/linux/**
        retention-days: 14

    # compare (or generate) failure → upload report + actual/expected/diff PNGs.
    - name: Upload Playwright report + diffs
      if: failure()
      uses: actions/upload-artifact@v7
      with:
        name: visual-linux-report
        path: artifacts/playwright
        retention-days: 7

  visual-windows:
    name: Visual regression (Windows baselines)
    # Opt-in only, and COMPARE-ONLY BY CONSTRUCTION: this job has no generate
    # mode and never passes --update-snapshots. CI must not be able to write a
    # pixel, so the only way win32 baselines change is a local run committed by
    # the owner after a by-eye review (docs/visual_baseline_win32/EVIDENCE.md).
    #
    # Not a required PR check: workflow_dispatch-only, and an if:-gated job
    # cannot block merge. Promoting it to one is a separate owner decision that
    # is BLOCKED on the mismatch documented below.
    if: ${{ inputs.run_visual_windows }}
    #
    # PINNED to windows-2022 (NOT windows-latest, which frozen-windows uses):
    # a byte comparison is only meaningful when the renderer matches the one
    # that produced the baselines -- Chromium build + installed font set +
    # DirectWrite. `windows-latest` is a moving alias, so an image promotion
    # would silently move fonts and red the gate by construction. This is the
    # same reasoning the visual-linux job records for ubuntu-24.04.
    #
    # KNOWN MISMATCH, stated rather than discovered later: unlike the Linux
    # corpus, which this workflow's `generate` mode produced on ubuntu-24.04,
    # `e2e/__screenshots__/win32/` was generated on the OWNER'S LOCAL Windows
    # machine (#309). A runner is not that machine, so this job's first compare
    # is a MEASUREMENT of that delta, not a pass/fail verdict on the code under
    # test. Read a red here as "runner rendering differs from local rendering"
    # until someone proves otherwise. Making this a blocking gate requires the
    # corpus to be regenerated ON this runner image and reviewed -- which is an
    # owner decision, not a side effect of adding this job.
    runs-on: windows-2022

    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version-file: '.python-version'
        cache: 'pip'

    - name: Set up Node.js
      uses: actions/setup-node@v7
      with:
        node-version: '24'
        cache: 'npm'

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Install npm dependencies
      run: npm ci

    - name: Build Bootstrap-customized CSS
      run: npm run build:css

    - name: Install Playwright Chromium
      run: npx playwright install --with-deps chromium

    # Same fail-fast as visual-linux: without the committed fixture,
    # prepare_visual_db.py falls back to DEFAULT_SOURCE (data/database.db) and
    # could render real data into a comparison.
    - name: Assert committed visual seed present
      run: if (-not (Test-Path 'e2e/fixtures/database.visual.seed.db')) { throw 'visual seed fixture missing' }
      shell: pwsh

    # PW_DB_PATH is the repository's established isolation mechanism -- the same
    # variable run-playwright-shards.ps1 gives each shard, asserted by
    # tests/test_playwright_shard_launcher_contracts.py. It resolves under the
    # gitignored artifacts/ tree, and prepare_visual_db.py refuses live-data
    # paths, so data/database.db is never opened, let alone written.
    #
    # PW_PORT avoids 5060/5061: Chromium treats those as restricted (SIP) and
    # fails every navigation with ERR_UNSAFE_PORT before a single pixel is
    # captured -- measured 2026-08-10, 66/66 failed that way on 5061.
    - name: Run Windows visual specs (compare only)
      env:
        PW_VISUAL_SEED: '1'
        PW_PORT: '5057'
        PW_DB_PATH: 'artifacts/e2e/database.visual-windows.db'
      run: npx playwright test --project=chromium e2e/visual.spec.ts e2e/visual-baseline-thumbnails.spec.ts
      shell: pwsh

    # Playwright CREATES a missing baseline instead of failing, so a green run
    # is not by itself proof that nothing was written. This makes that explicit:
    # any change under e2e/__screenshots__ fails the job even when the specs
    # passed, which is the mechanical form of "CI never pushes pixels".
    - name: Assert no baseline was written
      if: always()
      run: |
        $changed = git status --porcelain -- e2e/__screenshots__
        if ($changed) { Write-Host $changed; throw 'CI wrote to e2e/__screenshots__; baselines are owner-local and must never be produced here' }
      shell: pwsh

    - name: Upload Playwright report + diffs
      if: failure()
      uses: actions/upload-artifact@v7
      with:
        name: visual-windows-report
        path: artifacts/playwright
        retention-days: 7

  dependency-health:
    name: Dependency Health Check
    runs-on: ubuntu-latest

    # Moved out of the required PR pipeline (Phase 4.5) — outdated/safety scans are
    # informational and don't belong on the critical merge path.
    steps:
    - name: Checkout code
      uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version-file: '.python-version'

    - name: Check for outdated packages
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip list --outdated
      continue-on-error: true

    - name: Check package vulnerabilities
      run: |
        pip install safety
        safety check --json || true
      continue-on-error: true
