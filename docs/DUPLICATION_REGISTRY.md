# Duplication & Propagation Registry (WP2.5)

Behavior-preserving inventory of intentional and incidental duplication across the
codebase. Per Refactor Plan v3 §WP2.5, this is a **DECISION LEDGER, not a consolidation
plan** — the binding rule is: *"Do not consolidate protected logic merely because
arithmetic looks identical."* Each entry records: owner (canonical location), instances
(`file:line`), current semantics, guarding tests, and an explicit future decision.

**Scope of the WP that produced this file:** docs-only. Zero production/test/E2E code was
changed. No duplication was removed. Basis: `docs/SCAN_FINDINGS.md` +
`docs/scan/PHASE_*.md` (2026-07-03 line-by-line grounding scan) cross-checked against a
fresh code read (2026-07-10). Every instance below cites a live `file:line`.

**Decision vocabulary:**

- **KEEP SEPARATE** — semantics genuinely differ or logic is protected; do not merge.
- **DEFER (WPx)** — consolidation is plausible but belongs to a later, gated WP.
- **DO NOT CHANGE** — owner-gated behavior; touching it is a behavior change, not a refactor.
- **CONSOLIDATE (WPx)** — the *only* rows where merging is planned; each removes real drift,
  not arithmetic look-alikes.

Of the 14 items, **exactly one** (#14, schema manifests) was slated for consolidation;
that work **shipped as WP2.6** (`utils/schema_registry.py`, `run_all_initializers()`).
No row recommends merging protected logic on arithmetic-similarity grounds.

> **Pointer freshness.** **Rows 3, 11 and 14 only** were re-derived against
> `db1bc5d` (2026-08-04); the 2026-08-01 audit's findings for those rows, and the
> summary/row-4 disagreement, are corrected in place. **Other rows are not
> guaranteed fresh** — row 9's `volume-splitter.js` pointers and row 13's
> `conftest.py:280` are known to have drifted. Line numbers move with every merge:
> re-derive before relying on one.

---

## Registry

| # | Item | Owner (canonical) | Instances (`file:line`) | Current semantics | Guarding tests | Future decision |
|---|------|-------------------|-------------------------|-------------------|----------------|-----------------|
| 1 | Fatigue scored-row + sort tie-break | `utils/_fatigue/` (calc pipeline); `utils/fatigue_data.py` (page merge) | scored-row: `_fatigue/period.py:150` `adapt_logged_row` (`has_any_scored`:175) vs `fatigue_data.py:208` `_stimulus_from_rows` (`has_any_scored`:230); tie-break: `_fatigue/per_muscle.py:210` `summarize_muscle_bars` vs `fatigue_data.py:265` `_merge_muscle_rows` (sort:308) | `has_any_scored` over scored_rir/min/max/weight, then scored→planned fallback. Sort key `(has_landmarks, -%MRV, -score, muscle)`. **calc = single side**; **page-merge = max-of-two-sides** (planned vs logged) — the `-max_percent_of_mrv/-max_score` variant | `test_fatigue.py`, `test_fatigue_golden.py`, `test_fatigue_routes.py`, `test_fatigue_context.py` | **KEEP SEPARATE.** Explicitly deferred by WP2.4. Protected calc zone; semantics differ (single-side vs max-of-two-sides), so identical-looking arithmetic is coincidental. Merge only under a dedicated fatigue-dedup WP with golden + product-risk gate. |
| 2 | Estimator ↔ calibration load-basis factor | Two owners: `_profile_estimator/core_math.py::_load_basis_factor` (estimator) and `strength_calibration.py::_LOAD_BASIS_FACTOR`/`_promotion_basis_factor` (calibration) | `_profile_estimator/core_math.py:103`; `strength_calibration.py:222` (dict), `:346` (consume), `:523` `_promotion_basis_factor`, `:528` xref comment → estimator; consumer `_profile_estimator/traces.py:433`. Vocabulary pinned by DB CHECK `utils/database.py:743-744` (`load_basis IN (...)`) | per-hand ↔ per-side/total load conversion factor. Estimator derives it from `reference_lift_key`+`target_is_per_hand`; calibration maps stored `load_basis` string → factor and computes a source→target promotion factor | `test_profile_estimator.py`, `test_profile_estimator_contract.py`, `test_strength_calibration.py` | **DEFER.** Already cross-referenced (`strength_calibration.py:528`). Consolidation couples two protected zones across the deliberate estimator↔calibration lazy-import boundary (scan Phase 5). Revisit only if a 3rd copy appears or WP2.6 touches the `load_basis` enum. |
| 3 | Weekly ↔ session aggregation + movement-pattern fork | Separate: `weekly_summary.py` (weekly agg), `session_summary.py` (session agg); shared only `EFFECTIVE_STATUS_MAP` (owned by `weekly_summary.py`) | `weekly_summary.py:217` `calculate_weekly_summary`, `:368` `_infer_pattern`, `:504` `calculate_pattern_coverage` (inline substring elif fork); `session_summary.py:251` `calculate_session_summary`; import `session_summary.py:9` (map only) | **NOT duplicates** (weekly = across-routine + frequency, no date filter; session = per-routine + optional date + per-session avgs; CLAUDE.md §5). Shared risk = pattern-inference fork: `PatternMapping.MUSCLE_GROUP_PATTERNS` vs inline elif fallback, which drift independently (scan Phase 3). WP2.3 golden locked the `'Calves' !⊃ 'calf' → 'other'` quirk | `test_weekly_summary*.py`, `test_session_summary*.py` (+ goldens) | **KEEP SEPARATE** (aggregation, per CLAUDE.md §5). Pattern fork: **DEFER** consolidation — golden-locked quirk; unify only under a movement-pattern WP with golden refresh. Note: `effective_sets.py` second pipeline was the intended unifier but is **DEAD** (scan Phase 3) — do not resurrect. |
| 4 | Routine normalization (falsy-routine seam) | `'Unassigned'` for the two summary consumers; fatigue still diverges. This was the OD4/WPB.4 seam | `weekly_summary.py:77,148,252,401`; `session_summary.py:40,60,90`; `fatigue_data.py:75,100,123` | `routine` is `TEXT NOT NULL`, so `''` is the only reachable falsy case; such rows arrive from restored backups, legacy data, or direct edits. **Converged 2026-08-01 (WPB.4):** weekly now buckets falsy rows into one synthetic `'Unassigned'` session (`weekly_summary.py:77,148`), matching session (`session_summary.py:90`). The WP2.3 golden was intentionally regenerated and now LOCKS the bucket. **Still divergent:** `fatigue_data.py` coalesces to a `''` key, and pattern coverage (`weekly_summary.py:401`) deliberately keeps keying falsy routines as `''` | summary/fatigue tests + goldens | **Three conventions became two.** Owner decision **D1** deliberately retains one asymmetry: `global_sessions` (`weekly_summary.py:252`) still **excludes** the synthetic bucket, so it stays the fallback denominator only for muscles clearing `>= 1.0` in no session — including it would perturb every unrelated low-frequency muscle. Unifying `fatigue_data.py` is a **separate** behavior change needing its own goldens and the calibration freeze respected; do not fold it in opportunistically. |
| 5 | JS/Python taxonomy lists | Python canonical: `constants.py` (muscle groups), `normalization.py` (canonical maps), `volume_taxonomy.py:88` `COARSE_TO_BASIC`, `:286` `_PST_ALIASES` | JS copies: `muscle-selector.js:30` `SIMPLE_TO_ADVANCED_MAP`, `:73` `MUSCLE_LABELS`, `:110` `MUSCLE_TO_BACKEND`; `user-profile.js:30` `COLD_START_CANONICAL_COMPOUND`, `:77-80` `COHORT_*`; `filter-view-mode.js:29/56/116` `SIMPLE/ADVANCED_MUSCLES`, `DB_TO_SIMPLE`; `routine-cascade.js` routine names vs `plan_generator.py` (scan Phase 15) | Muscle simple↔advanced↔DB label maps + cohort constants hand-synced across Python and ≥3 JS modules; no automated parity test for most (only `muscle-selector` has `TestRegionVisualState`, scan Phase 13) | `test_volume_taxonomy.py` (Py; reads LIVE db — non-hermetic, scan Phase 21), `test_muscle_selector_mapping.py` (partial mirror) | **DEFER (WP3.x).** Cross-language duplication cannot be consolidated in the Python phase. Durable fix = generated/shared taxonomy source + parity tests. Add drift-guard parity tests before any JS module split. No Python-side change. |
| 6 | Assisted-bodyweight names + fatigue landmark coverage | `workout_log.py:7` `ASSISTED_BODYWEIGHT_EXERCISES` (6 names); `_fatigue/per_muscle.py:51` `MUSCLE_VOLUME_LANDMARKS` (12 muscles) | `workout_log.py:7-25`; `per_muscle.py:51-64` (Chest, Latissimus-Dorsi, Middle-Shoulder, Biceps, Triceps, Quadriceps, Hamstrings, Glutes, Calves, Abdominals, Traps, Forearms = **12 of ~18** canonical; 6 gaps → `band=None`/"—"). JS mirror: assisted case triplicated `workout-log.js:388,580,801` (one copy omits it, scan Phase 15) | `test_workout_log*`, `test_fatigue*` (progression-badge JS drift untested) | **KEEP SEPARATE + FLAG staleness.** Curated domain lists, not arithmetic dupes — do not auto-derive. Add catalog-parity tests (assisted ⊆ exercise catalog; landmark keys vs canonical muscle set) in a future test-hardening WP. The 6 landmark gaps are intentional (no §5 data) — document, don't fill. |
| 7 | Server-data-to-JS conventions | None — three coexisting conventions | JSON `<script type="application/json">`: `templates/user_profile.html:280` (`coverage\|tojson`); `tojson`-in-`data-*`: `templates/volume_splitter.html:25-28`; plain `data-*`: `templates/body_composition.html:10-15` (scan Phase 11) | Three ways to hand server data to JS; no single documented convention | E2E page specs (indirect) | **DEFER (WP3.x/WP4).** Pick one convention (JSON script tag for structured payloads, `data-*` for scalars) during template work. No change now. |
| 8 | Static asset / cache policy | **RESOLVED 2026-08-01** — `utils/version.py` `APP_VERSION`; `app.py` `apply_static_cache_policy()`; `templates/base.html` links carry `?v={{ app_version }}` | `app.py` **no longer sets `SEND_FILE_MAX_AGE_DEFAULT` at all** — it sets `STATIC_LONG_MAX_AGE` and narrows per request. All 33 first-party CSS/JS links carry `?v={{ app_version }}`; 0 unversioned, 0 random busters | Was: 1-year immutable caching without content-hashed filenames → stale-asset risk. **Closed** by app.py-review P4 (`16a4e53`, #236) plus the residual fix `1619262` (#262), which narrowed the long cache to the current `APP_VERSION` and made everything else revalidate — covering transitive ES-module imports and runtime-built URLs, which can never carry a `?v=` | none direct | **DONE — do not re-implement.** The old "DEFER (WP3.0/WP4 asset pipeline) — add content-hash/versioned asset URLs" instruction is **discharged**; a `packaged-smoke-windows` CI job now asserts the policy on every PR. Retained for provenance. |
| 9 | Volume-splitter silent failures | `routes/volume_splitter.py` + `utils/volume_splitter.py` (server); `static/js/modules/volume-splitter.js` (client) | Server broad `except Exception`: `routes/volume_splitter.py:58,108,117,144,155,166,177,187` → `logger.exception` + `error_response`. Client `volume-splitter.js:179` `.catch` **console.error only** (no toast) on `/api/calculate_volume`; `:16-90` reimplements `apiFetch` envelope logic (scan Phase 14) | Server logs + returns error envelope; client **silently swallows** failures (console only) — user sees nothing on calculate failure | `test_volume_splitter_api.py`, `test_volume_ai.py` (no JS test for silent catch) | **DEFER — this is a BUG, not intentional duplication.** Route to bug ticket / WP3.5 (delete `volume-splitter.js:16-90` apiFetch reimpl wholesale, not call-site swap, per scan Phase 14). No fix in this docs-only WP. |
| 10 | Backup refresh/confirmation interaction | `static/js/modules/backup-center.js` + `program-backup.js`; `routes/program_backup.py` | `backup-center.js:151-161` confirm UI (`backup-action-confirm(-btn)`), `:173+` restore-result; `program-backup.js` refresh flow; `program-backup.js:104-162` `showAutoBackupBanner` **orphan** (zero call sites) (scan Phase 14) | Background refresh can dismiss an in-flight restore confirmation → UX regression; orphaned auto-backup banner never wired | e2e `backup.spec`, `test_program_backup.py` (server only) | **DEFER (WP3.5).** UX bug + dead feature, not arithmetic duplication. Wire-or-delete the orphan banner and fix the refresh/confirm race under JS work. No change now. |
| 11 | Response-helper return-type asymmetry | `utils/errors.py` — `success_response`:22 vs `error_response`:67 | `errors.py:22` `success_response` → **plain `Dict`** (caller must `jsonify`, e.g. `app.py:265`); `:67` `error_response` → **`(jsonify(...), status_code)` tuple**. Intentional 200-with-`ok:false`: replace-exercise `NO_CANDIDATES`/`DUPLICATE`/`SELECTION_FAILED` pass `status_code=200` (CLAUDE.md §5) | Asymmetric contract (bare dict vs response tuple); easy to misuse (forgot-to-jsonify). The 200+`ok:false` cases are intentional "couldn't process" outcomes | `test_errors_utils.py`; replace-exercise route tests assert 200+`ok:false` | **KEEP AS-IS (documented).** CLAUDE.md §5 records the exceptions as intentional. Harmonizing `success_response` to a tuple is an API-contract change touching every route (refactor invariant → migration notes). **DEFER** to a dedicated response-contract WP; do NOT fold into a structural move PR. |
| 12 | Protected calibration helpers | `utils/strength_calibration.py` (learned-calibration + observer pipeline) | `strength_calibration.py:170` `get_learned_calibration`, `:186` `list_learned_calibrations`, `:278` `get_related_calibration_candidate` (107-line Phase-2A math, scan Phase 6), `:523` `_promotion_basis_factor`; Stage-4 observer artifacts | Learned-calibration advisory math with safe default-off + regression guard; observer = Stage-4 evidence gathering. 2D-D (suggestion-number modification) is **BLOCKED** (memory `project_learned_calibration_phases.md`) | `test_strength_calibration.py`, `test_fatigue_stage4_observer.py`, `test_fatigue_context.py` | **KEEP SEPARATE / DO NOT REFACTOR** without owner approval. Protected zone; 2D-D BLOCKED. Any decompose of `get_related_calibration_candidate` needs product-risk-reviewer gate (scan Phase 6). No change. |
| 13 | Persisted `user_selection` field propagation | Canonical schema `utils/db_initializer.py:184` (`user_selection` CREATE) + startup-added columns | Field set: `routine, exercise, sets, min_rep_range, max_rep_range, rir, rpe, weight` (`db_initializer.py:184`), `superset_group` (`:205` ALTER), `execution_style` (`:210` ALTER), `exercise_order` (`routes/workout_plan.py::initialize_exercise_order` startup ALTER). Propagation: backup mirror `program_backup.py:56-65` (+ superset migration `:70-75`, order check `:173`); `export_service.py` + `export_utils.py`; `workout_log.py`; `tests/conftest.py:280` INSERT (**omits** superset_group/exercise_order/execution_style → defaults) + E2E seeds | A column added to `user_selection` must propagate to backup schema, export column maps, log queries, and every test/E2E fixture INSERT — else backup/export silently drops it | `test_program_backup.py`, `test_exports.py`, e2e backup/export specs | **KEEP (intentional propagation) + register the full field set as a CHECKLIST.** Recommend a schema-owned column manifest under WP2.6 so backup/export **derive** columns instead of hardcoding. No consolidation now. |
| 14 | Production/test/E2E schema & user-state manifests | **`utils/schema_registry.py`** — `run_all_initializers()` (`:115`) is the single producer | **Prod:** `app.py:19` imports it; `app.py:95` `run_all_initializers(force_base=False)`; `erase_data` at `app.py:246`. **Test:** `tests/conftest.py:24` import, `:36` and `:121` `run_all_initializers(force_base=True)`. **E2E:** `prepare_visual_db.py:87,93` and `seed_summary_regression_db.py:29,31` call it directly; `prepare_e2e_db.py` reaches it via `prepare_visual_db.apply_migrations`. **`build_visual_seed.py` does not** — it copies the prebuilt `e2e/fixtures/database.visual.seed.db` and still owns one raw `ALTER TABLE exercises ADD COLUMN media_path` (`:65-68`, called at `:166`) duplicating `db_initializer.py:121-122`. That is the one residual seam | **CONSOLIDATED.** The three hand-maintained init sequences are gone; every consumer calls one registry. The drift this row was opened for — no `e2e/scripts/*.py` calling `add_fatigue_context_settings_table`, so E2E DBs silently lacked `fatigue_context_settings` — is closed by construction, because the registry owns the table list rather than each caller repeating it | full pytest (conftest path), E2E suite (scripts path) | **DONE — shipped as WP2.6.** Root `CLAUDE.md` §2 documents the startup sequence. This row is completion history, not open work: **do not re-implement WP2.6**, and do not read "CONSOLIDATE" here as a pending action. |

---

## Summary of dispositions

| Decision | Items |
|---|---|
| KEEP SEPARATE (protected / genuinely different) | 1, 3 (aggregation), 6, 11, 12 |
| DO NOT CHANGE (owner-gated behavior) | 4 — converged 2026-08-01 via WPB.4; see row 4 |
| DEFER to a later gated WP | 2, 3 (pattern fork), 5, 7, 8, 9, 10, 11 |
| CONSOLIDATED (shipped) | 14 — WP2.6 schema registry |
| KEEP + checklist / parity-test recommendation | 6, 13 |

**No row recommends consolidating protected logic on arithmetic-similarity grounds.** The
fatigue scored-row/tie-break (#1) and estimator/calibration load-basis (#2) — the two most
tempting "arithmetic looks identical" candidates — are both explicitly held separate. The
single consolidation (#14) targeted hand-maintained schema-init sequences whose drift was
already observable (missing `add_fatigue_context_settings_table` in E2E); it **shipped as
the WP2.6 schema registry** and is recorded above as done.
