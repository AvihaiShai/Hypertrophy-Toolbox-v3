# Opus new-session prompt — continue WP4.4 i → j → k

Use the text below as the normal kickoff message in a fresh Opus chat. Register the short
`/goal` shown after it; the full authority lives in the repository to avoid the 4,000
character goal-condition limit.

---

You are the manager continuing WP4.4 from the active Packet-i worktree through i → j → k.

Read, in order and in full:

1. `CLAUDE.md`
2. `docs/MASTER_HANDOVER.md`
3. `docs/ACTIVE_DEVELOPMENT.md`
4. `docs/css_phase4_wp4_4/PLANNING.md`
5. `docs/css_phase4_wp4_4/N4_CONTINUATION_AUTHORITY.md`
6. `docs/css_phase4_wp4_4/EXECUTION_HANDOFF_I_K.md`
7. both `docs/CSS_PHASE4_WP4_4_N4_INVENTORY_*.md`
8. `docs/CSS_PHASE4_WP4_4_H_COMPONENTS_DEAD_EVIDENCE.md`
9. the current i evidence, contracts and audit scripts in
   `D:/development/Hypertrophy-Toolbox-v3-main-wp4-4-i-is-repair`
10. `docs/ai_workflow/PARALLEL_WORKFLOW.md` and `WORKSTREAM_OWNERSHIP.md`

The continuation-authority document is the owner's fresh N4 decision. It supersedes stale
sentences saying N4 is unapproved or no packet is next. Do not ask for those decisions
again. Follow its pre-answered decision matrix and stop only for its genuine hard stops.

Start with a read-only state audit:

- fetch origin and verify current main is `1019d34` or a descendant containing PR #211;
- preserve the owner's dirty main-checkout `CLAUDE.md` exactly;
- list all worktrees, branches, statuses, running Flask/Node/Playwright processes and port
  listeners before running another oracle;
- inspect `wt/wp4-4-i-is-repair` without discarding anything;
- verify which files are committed in the WIP snapshot and which are working-tree
  experiments; do not mistake an active negative control for the intended repair;
- verify every evidence artifact by checkout root, source commit, **served** CSS digest and
  DB digest rather than by its directory name.

Before resuming i, land the prepared docs-only authority/status sync described in the
handoff. Stage exactly its seven documentation paths and never stage `CLAUDE.md`. Rebase i
onto that merge. Apply authority ruling C16 so an identical-input docs-only rebase does not
cause blind reuse or an unnecessary full rerun.

Critical current correction: a G3 run launched from the i worktree is the **after** half
even if its output path says “before.” Complete or terminate the existing run safely,
relabel/discard it from metadata, and run the true before half from pristine current main.
The final comparison must use current-main before versus final-i after. Do not cite any
pre-#211 result as a final gate.

Use multiple agents immediately under the authority file's model: parallel read-only
cascade/contract/evidence review, one manager writer, and serialized DB/server/browser
jobs. Continue autonomously through PR creation, CI and merge. After i resolves, arrange j
from merged main through the external worktree workflow; after j merges, do the same for k.
Managers/subagents do not create, remove or move worktrees. Do not reopen the 235
declarations or any parked work.

Report concise milestone updates, not routine approval questions. Finish only at the
authority document's Definition of Done. Update `EXECUTION_HANDOFF_I_K.md` after every
material result, PR transition, merge/rebase and before session end.

---

## Short goal invocation (well below 4,000 characters)

```text
/goal Complete and merge WP4.4 i → j → k under docs/css_phase4_wp4_4/N4_CONTINUATION_AUTHORITY.md, using docs/css_phase4_wp4_4/OPUS_CONTINUATION_PROMPT.md as the session handoff. Use parallel read-only subagents with one writer and serialized runtime gates. Continue through PRs, CI, merges and canonical closeout; stop only for the authority file's genuine hard stops.
```
