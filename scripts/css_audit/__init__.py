"""WP4.4 shared-surface CSS audit harness.

Committed under ``scripts/`` (not ``artifacts/``) so the packets that depend on
WP4.4-a can re-run it; every *generated* capture and report stays under the
gitignored ``artifacts/wp4_4/``.
"""
